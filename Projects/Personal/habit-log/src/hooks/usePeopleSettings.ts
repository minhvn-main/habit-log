// Re-export everything from the context
export { 
  usePeopleSettings, 
  isPresetColor, 
  isValidHex, 
  PRESET_COLORS,
  type PresetColor,
  type PersonColor 
} from '@/contexts/PeopleSettingsContext';
